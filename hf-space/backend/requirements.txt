# Production runtime dependencies only.
# Dev/lint/test tooling (pytest, black, isort, flake8, mypy) and unused SDKs
# (boto3, typer, jq, pandas, requests-oauthlib) are deliberately excluded --
# they added build time and image size without being imported anywhere.

# API framework
fastapi==0.110.1
uvicorn[standard]==0.25.0
python-multipart>=0.0.9

# Database
motor==3.3.1
pymongo==4.5.0

# Models / validation
pydantic>=2.6.4
email-validator>=2.2.0

# Auth
pyjwt>=2.10.1
bcrypt==4.1.3
passlib>=1.7.4
python-dotenv>=1.0.1

# Matching engine
sentence-transformers>=3.0.0
scikit-learn>=1.5.0
numpy>=1.26.0
thefuzz>=0.22.1
python-Levenshtein>=0.25.0

# OCR
pillow>=10.0.0
pytesseract>=0.3.10
